V22.3 FINAL FIX2

The previous workflow failed because it compared a SHA-256 digest
against a non-SHA256 model identifier.

The official whisper.cpp model table currently lists a 40-character
model hash for ggml-large-v3-turbo-q5_0, so the previous 64-character
SHA256 check was invalid.

FIX2 removes that brittle digest comparison and instead verifies:
- the model file exists
- the model file is at least 500 MB
- whisper.cpp DTW smoke test runs successfully

All V22.3 subtitle and Countdown-intro code remains unchanged.
